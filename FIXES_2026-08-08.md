# Celine Closet - UX & bug fixes 08/08/2026

## Đã sửa theo ghi chú

1. **Thêm vào giỏ không rời trang sản phẩm**
   - `/cart` hỗ trợ AJAX JSON cho thao tác add.
   - Trang chi tiết, danh sách sản phẩm và wishlist thêm nhanh nhưng vẫn đứng nguyên trang.
   - Có hiệu ứng ảnh sản phẩm bay vào biểu tượng giỏ, cập nhật badge và toast kết quả.
   - `Mua ngay` vẫn đi checkout như cũ.
   - Giỏ vẫn hỗ trợ nhiều sản phẩm / nhiều biến thể theo màu + size như logic hiện có.

2. **Trong giỏ có thể bấm lại sản phẩm**
   - Ảnh/tên sản phẩm trong giỏ dẫn về `/product-detail?id=...`.

3. **Lượt mua trên chi tiết sản phẩm**
   - Hiển thị số lượng đã bán từ các đơn `Hoàn thành`.

4. **Voucher checkout**
   - Bấm mã gợi ý chỉ chọn/điền mã.
   - Chỉ tính và trừ ưu đãi khi bấm `Áp dụng` (hoặc Enter tại ô mã).

5. **Tách quyền đơn hàng của STAFF**
   - STAFF chỉ thấy/mở/theo dõi/cập nhật đơn có `maNhanVienXuLy` của chính mình.
   - ADMIN vẫn xem toàn bộ; DELIVERY chỉ xem đơn được giao cho mình.

6. **Khách xác nhận Đã nhận hàng**
   - Khi đơn `Đang giao`, khách có nút `Đã nhận hàng`.
   - Sau xác nhận: đơn `Hoàn thành`; COD được ghi nhận PAID; cộng điểm; báo nhân viên liên quan.

7. **Đánh giá sau mua**
   - Rating đổi thành 5 sao bấm trực tiếp.
   - Giữ upload ảnh đánh giá và kiểm tra đơn hoàn thành như logic có sẵn.

8. **Chat gợi ý sản phẩm**
   - Backend chọn sản phẩm đang bán trong DB theo nội dung tư vấn.
   - API chat trả thêm danh sách sản phẩm.
   - Chatbox hiển thị card ảnh + tên + giá bên dưới; bấm mở thẳng chi tiết sản phẩm.

9. **Tin tức**
   - Thêm `/news-detail?id=...`.
   - Card tin tức bấm được ảnh, tiêu đề và `Đọc bài viết`.
   - Trang chi tiết có ảnh, nội dung, ngày cập nhật và tin liên quan.

10. **Trang Giới thiệu**
    - Nâng cấp bố cục editorial: hero, số liệu/dịch vụ, câu chuyện, giá trị, quy trình, gallery và CTA.

11. **Đổi mật khẩu khách hàng**
    - Thêm biểu tượng mắt hiện/ẩn cho cả mật khẩu mới và xác nhận mật khẩu.

## Kiểm tra kỹ thuật đã thực hiện

- `node --check` cho `assets/js/main.js`: đạt.
- Kiểm tra syntax Java toàn bộ source bằng `javac --release 17`; không phát hiện lỗi cú pháp Java. Môi trường kiểm tra không có Maven/dependency Jakarta/Hibernate nên chưa thể thực hiện Maven build đầy đủ tại đây.
- Kiểm tra cân bằng các JSTL block ở các JSP đã sửa.
- Không thêm bảng/cột database mới.

## Sau khi import Eclipse

- Maven > Update Project.
- Project > Clean.
- Chạy bằng Tomcat 11 / Java 17.
- Hard refresh trình duyệt (Ctrl+F5) vì CSS/JS đã đổi cache key sang `ux-fixes-20260808`.
